name: Push on Epitech repository

on:
  push:
    branches:
      - dev      
jobs: 
  build:
    if: github.repository == 'PlockGames/plock_mobile'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
          token: ${{ secrets.AUTOMNE_TOKEN }}

      - name: Git clone
        run: |
          git clone git@github.com:EpitechMscProPromo2025/T-ESP-800-project_esp800-56365-TLS-tiktok_game.git repo
          cd repo
          mkdir -p back
          cd back
          touch test
          ls

      # - name: Commit files
      #   run: |
      #     git config --local user.email "github-actions[bot]@users.noreply.github.com"
      #     git config --local user.name "github-actions[bot]"
      #     git checkout $GITHUB_REF
      #     git push --force https://github.com/EpitechMscProPromo2025/T-ESP-800-project_esp800-56365-TLS-tiktok_game.git $GITHUB_REF
        env:
          GITHUB_REF: ${{ github.ref }}
