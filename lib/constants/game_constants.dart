import 'package:flame/components.dart';

/// Constantes pour la résolution des jeux
class GameConstants {
  /// Résolution standard pour tous les jeux (largeur: 360, hauteur: 640)
  static final Vector2 standardResolution = Vector2(360, 640);

  /// Ratio d'aspect standard (9:16 pour format portrait)
  static const double standardAspectRatio = 9 / 16;
}